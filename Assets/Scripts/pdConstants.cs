public class pdConstants
{
    /// <summary>
    /// 必须从0开始自增加
    /// </summary>
	public enum UIPanelName : int
    {
        TestEsc,

        /// <summary>
        /// 必须放在最后，用于遍历UIPanel
        /// </summary>
        Count,
    }
}